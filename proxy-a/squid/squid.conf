http_port 3128 transparent

# 上位のプロキシサーバーの設定
cache_peer proxyb parent 3128 0 no-query default

# 一部のローカルネットワークを許可する（必要に応じて変更）
acl localnet src 172.17.0.0/16
http_access allow localnet

# 他のアクセスを拒否
http_access deny all

# DNSの設定
dns_nameservers 8.8.8.8 8.8.4.4

# その他の標準的な設定（ログファイルの場所、キャッシュのサイズなど）

